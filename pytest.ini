[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: tests sin base de datos (transformadores)
    integration: tests contra BD de prueba individual por módulo
    pipeline: tests end-to-end del pipeline completo
